services:
  - type: web
    name: gameforge
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: gameforge-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
